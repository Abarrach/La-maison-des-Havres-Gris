# Données runtime / fichiers sensibles (jamais sur GitHub)
users_SECURE_9x.json
requetes.json
bases.json
migration_data.json

# État local Claude Code (worktrees, settings personnels)
.claude/

# Secret webhook Discord (NE JAMAIS COMMITER)
discord_webhook.txt

# Secret OAuth2 "Login Discord" — client_secret + bot_token (NE JAMAIS COMMITER ; voir discord_oauth_config.example.php)
discord_oauth_config.php

# Secrets Retours de soirée — clé Gemini + token (NE JAMAIS COMMITER ; voir epice/config.example.php)
epice/config.php

# Secret bot Sorties Discord — bot_token (NE JAMAIS COMMITER ; voir epice/discord_sortie_config.example.php)
epice/discord_sortie_config.php
# Journal runtime du bot Sorties
epice/discord_sortie.log

# Données runtime des retours de soirée (recréé par data-api.php)
epice/data/debriefs.json
epice/data/organizers.json
# Sorties Discord non-épice (Labo/Farm/Landsraad) — runtime, recréé par discord_sortie.php
epice/data/discord_sorties.json

# Meshes du jeu extraits via FModel (lourd ~357Mo + assets Funcom) — déploiement WinSCP séparé
models/
*.glb

# Caches runtime des proxies gaming.tools (recréés par les .php ; ne pas commiter)
dd_cache.json
stuff_cache_list.json
stuff_cache_skills.json
stuff_cache/
# Cache de noms d'items (intermédiaire build, régénéré par build_stuff_snapshot.js)
stuff_names.json
